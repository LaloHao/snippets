# -*- mode: snippet -*-
# name: ReactNative#Component
# key: rnc
# --
/*
 * `(file-name-nondirectory (buffer-file-name))`
 */

import React from 'react';
import PropTypes from 'prop-types';

import {
  Colors,
  View,
  Text,
  HeightUnit,
  WidthUnit,
} from 'common';

export class `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))` extends React.Component {
  render() {
    return (
      <View style={styles.container}>
        <Text
          value="Content"
        />
      </View>
        );
    }
}

const styles = {
  container: {
  },
};

`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`.propTypes = {

};

`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`.defaultProps = {

};
